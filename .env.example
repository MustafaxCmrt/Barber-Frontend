# Backend API base URL
# Empty = use Vite same-origin proxy (/api and /hubs), which works with tunnels.
VITE_API_BASE=

# Timezone (Bölüm 9 — UI saatleri TR olarak gösterilir)
VITE_TZ=Europe/Istanbul
